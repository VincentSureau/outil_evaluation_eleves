<form method="post" action="{{ path('admin_bordee_delete', {'id': bordee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bordee.id) }}">
    <button class="btn btn-danger">Delete</button>
</form>
