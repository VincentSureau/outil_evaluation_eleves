{% extends 'base.html.twig' %}

{% block title %}Bordee{% endblock %}

{% block body %}
    <h1>Bordee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bordee.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bordee.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('bordee_index') }}">back to list</a>

    <a href="{{ path('bordee_edit', {'id': bordee.id}) }}">edit</a>

    {{ include('back/bordee/_delete_form.html.twig') }}
{% endblock %}
