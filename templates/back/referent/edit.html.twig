{% extends 'base.html.twig' %}

{% block title %}Modifier un prof. principal{% endblock %}

{% block body %}

    <div class="container">

        <h1>Modifier un prof. principal</h1>

        <div class="card my-3">
            <div class="card-body">
                <p class="card-text text-right">
                    {{ include('back/referent/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                </p>

                <p class="card-text text-right">
                    {{ include('back/referent/_delete_form.html.twig') }}
                </p>
            </div>
        </div>

        <a href="{{ path('admin_referent_index') }}">retour à la liste</a>

    </div>
{% endblock %}
