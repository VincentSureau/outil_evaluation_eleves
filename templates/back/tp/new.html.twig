{% extends 'base.html.twig' %}

{% block stylesheets %}
    <link rel="stylesheet" href="{{ asset('select2/select2.min.css') }}">
{% endblock %}

{% block title %}Créer un TP{% endblock %}

{% block body %}

    <div class="container">

        <h1>Créer un nouveau TP</h1>

        <div class="card my-3">
            <div class="card-body">
                <p class="card-text text-right">
                    {{ include('back/tp/_form.html.twig', {'button_label': 'Enregistrer'}) }}
                </p>
            </div>
        </div>

        <a href="{{ path('admin_tp_index', { "id": specialisation.id }) }}">retour à la liste</a>

    </div>

{% endblock %}

{% block javascripts %}
    <script src="{{ asset('select2/select2.full.min.js') }}"></script>
    <script src="{{ asset('select2/init.js') }}"></script>
{% endblock %}