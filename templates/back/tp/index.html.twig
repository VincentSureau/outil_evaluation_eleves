{% extends 'base.html.twig' %}

{% block title %}Liste des TP{% endblock %}

{% block body %}
    <h1>Liste des TP</h1>

    <div class="text-right">
        <button class="btn btn-primary m-bot-10">
            <i class="fas fa-plus"></i>
            <a class="text-white" href="{{ path('admin_tp_new', { "id": specialisation.id }) }}">Ajouter un TP</a>
        </button>
    </div>
    
    <table id="tps_list" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Intitulé</th>
                <th>Spécialisation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('admin_specialisation_edit', {'id': specialisation.id}) }}">retour</a>
{% endblock %}

{% block javascripts %}

    <script type="text/javascript">
        var listUrl = '{{ path('api_tps_list', {'id': specialisation.id })|escape('js') }}';
    </script>
    <script type="text/javascript" src="{{ asset("js/back/tpDatatable.js") }}"></script>
{% endblock %}