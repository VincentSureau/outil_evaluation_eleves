{% extends 'base.html.twig' %}

{% block title %}New Student{% endblock %}

{% block body %}

    <div class="container">

        <h1>Create new Student</h1>

        <div class="card my-3">
            <div class="card-body">
                <p class="card-text text-right">
                    {{ include('back/student/_form.html.twig') }}
                </p>
            </div>
        </div>

        <a href="{{ path('back_admin_home') }}">back to list</a>

    </div>

{% endblock %}
