{% extends 'base.html.twig' %}

{% block title %}New Student{% endblock %}

{% block body %}

    <div class="container">

        <h1>Créer un nouvel élève</h1>

        <div class="card my-3">
            <div class="card-body">
                <p class="card-text text-right">
                    {{ include('back/student/_form.html.twig') }}
                </p>
            </div>
        </div>

        <a href="{{ path('back_admin_home') }}">retour à la liste</a>

    </div>

{% endblock %}