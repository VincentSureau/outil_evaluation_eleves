{% extends 'base.html.twig' %}

{% block title %}mei Task index{% endblock %}

{% block body %}
    <h1>Liste des tâches</h1>
    
    <div class="text-right">
        <button class="btn btn-primary m-bot-10">
            <i class="fas fa-plus"></i>
            <a class="text-white" href="{{ path('admin_task_new') }}">Ajouter une tâche</a>
        </button>
    </div>
    
    <table id="mei_tasks_list" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Référent</th>
                <th>Intitulé</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('back_admin_home') }}">retour</a>
{% endblock %}

{% block javascripts %}

    <script type="text/javascript">
        var listUrl = '{{ path('api_mei_tasks_list')|escape('js') }}';
    </script>
    <script type="text/javascript" src="{{ asset("js/back/meitaskDatatable.js") }}"></script>
{% endblock %}