{% extends 'base.html.twig' %}

{% block title %}Bac Pro MEI - Editer une tâche{% endblock %}

{% block body %}

    <div class="container">

        <h1>Editer une tâche</h1>

        <div class="card my-3">
            <div class="card-body">
                <p class="card-text text-right">
                    {{ include('back/meitask/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                </p>

                <p class="card-text text-right">
                    {{ include('back/meitask/_delete_form.html.twig') }}
                </p>
            </div>
        </div>

        <a href="{{ path('admin_mei_task_index') }}">retour à la liste</a>

    </div>
{% endblock %}
