{% extends 'base.html.twig' %}

{% block title %}Competence index{% endblock %}

{% block body %}
    <h1>Liste des compétences</h1>

    <div class="text-right">
        <button class="btn btn-primary m-bot-10">
            <i class="fas fa-plus"></i>
            <a class="text-white" href="{{ path('competence_new') }}">Ajouter une compétence</a>
        </button>
    </div>
    
    <table id="competence_list" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Reference</th>
                <th>Name</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('back_admin_home') }}">retour</a>
{% endblock %}


{% block javascripts %}

    <script type="text/javascript">
        var listUrl = '{{ path('api_competences_list')|escape('js') }}';
    </script>
    <script type="text/javascript" src="{{ asset("js/back/competenceDatatable.js") }}"></script>
{% endblock %}