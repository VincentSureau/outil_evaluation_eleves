{% extends 'base.html.twig' %}

{% block title %}New Specialisation{% endblock %}

{% block body %}

    <div class="container">

        <h1>Create new Specialisation</h1>

        <div class="card my-3">
            <div class="card-body">
                <p class="card-text text-right">
                    {{ include('back/specialisation/_form.html.twig') }}
                </p>
            </div>
        </div>

        <a href="{{ path('admin_specialisation_index') }}">back to list</a>

    </div>

{% endblock %}
