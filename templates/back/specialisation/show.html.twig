{% extends 'base.html.twig' %}

{% block title %}Specialisation{% endblock %}

{% block body %}
    <h1>Specialisation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ specialisation.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ specialisation.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_specialisation_index') }}">retour</a>

    <a href="{{ path('admin_specialisation_edit', {'id': specialisation.id}) }}">edit</a>

    {{ include('back/specialisation/_delete_form.html.twig') }}
{% endblock %}
