{% extends 'base.html.twig' %}

{% block title %}Specialisation index{% endblock %}

{% block body %}
    <h1>Specialisation index</h1>
    
    <a href="{{ path('admin_specialisation_new') }}">Create new</a>
    
    <table id="specialisations_list" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('back_admin_home') }}">retour</a>
{% endblock %}

{% block javascripts %}

    <script type="text/javascript">
        var listUrl = '{{ path('api_specialisations_list')|escape('js') }}';
    </script>
    <script type="text/javascript" src="{{ asset("js/back/specialisationDatatable.js") }}"></script>
{% endblock %}