{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
    {% if specialisation.id is not null %}
        {{ include('back/specialisation/_delete_form.html.twig') }}
    {% endif %}
{{ form_end(form) }}