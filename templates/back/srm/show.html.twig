{% extends 'base.html.twig' %}

{% block title %}Srm{% endblock %}

{% block body %}
    <h1>Srm</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ srm.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ srm.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ srm.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_srm_index') }}">retour</a>

    <a href="{{ path('admin_srm_edit', {'id': srm.id}) }}">edit</a>

    {{ include('back/srm/_delete_form.html.twig') }}
{% endblock %}
