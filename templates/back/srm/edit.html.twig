{% extends 'base.html.twig' %}

{% block title %}Edit Srm{% endblock %}

{% block body %}

    <div class="container">

        <h1>Editer un SRM</h1>

        <div class="card my-3">
            <div class="card-body">
                <p class="card-text text-right">
                    {{ include('back/srm/_form.html.twig', {'button_label': 'Update'}) }}
                </p>

                <p class="card-text text-right">
                    {{ include('back/srm/_delete_form.html.twig') }}
                </p>
            </div>
        </div>

        <a href="{{ path('admin_srm_index') }}">retour à la liste</a>

    </div>
{% endblock %}
