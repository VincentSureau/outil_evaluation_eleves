{% extends 'base.html.twig' %}

{% block title %}Srm index{% endblock %}

{% block body %}
    <h1>Listde des SRM</h1>
    
    <div class="text-right">
        <button class="btn btn-primary m-bot-10">
            <i class="fas fa-plus"></i>
            <a class="text-white" href="{{ path('admin_srm_new') }}">Ajouter un SRM</a>
        </button>
    </div>
    
    <table id="srm_list" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Email</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('back_admin_home') }}">retour</a>
{% endblock %}

{% block javascripts %}

    <script type="text/javascript">
        var listUrl = '{{ path('api_srms_list')|escape('js') }}';
    </script>
    <script type="text/javascript" src="{{ asset("js/back/srmDatatable.js") }}"></script>
{% endblock %}