{% extends 'base.html.twig' %}

{% block title %}Bac Pro SN - Modifier une tâche{% endblock %}

{% block body %}

    <div class="container">

        <h1>Editer une tâche</h1>

        <div class="card my-3">
            <div class="card-body">
                <p class="card-text text-right">
                    {{ include('back/sntask/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
                </p>

                <p class="card-text text-right">
                    {{ include('back/sntask/_delete_form.html.twig') }}
                </p>
            </div>
        </div>

        <a href="{{ path('admin_sn_task_index') }}">retour à la liste</a>

    </div>
{% endblock %}
