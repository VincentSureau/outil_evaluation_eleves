{% extends 'base.html.twig' %}

{% block title %}Task{% endblock %}

{% block body %}
    <h1>Task</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ task.reference }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ task.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_melec_task_index') }}">back to list</a>

    <a href="{{ path('admin_melec_task_edit', {'id': task.id}) }}">edit</a>

    {{ include('back/melectask/_delete_form.html.twig') }}
{% endblock %}
