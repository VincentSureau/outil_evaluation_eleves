{% extends 'base.html.twig' %}

{% block title %}Liste des lycées{% endblock %}

{% block body %}
    <h1>Liste des lycées</h1>
    
    <a href="{{ path('admin_school_new') }}">Ajouter un lycée</a>

    <table id="schools_list" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Ville</th>
                <th>Numéro</th>
                <th>Email</th>
                <th>Académie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('back_admin_home') }}">retour</a>
{% endblock %}

{% block javascripts %}

    <script type="text/javascript">
        var listUrl = '{{ path('api_schools_list')|escape('js') }}';     
    </script>
    <script type="text/javascript" src="{{ asset("js/back/schoolDatatable.js") }}"></script>
{% endblock %}