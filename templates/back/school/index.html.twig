{% extends 'base.html.twig' %}

{% block title %}School index{% endblock %}

{% block body %}
    <h1>School index</h1>
    
    <a href="{{ path('admin_school_new') }}">Create new</a>

    <table id="schools_list" class="table">
        <thead>
            <tr>
                <th>Prénom</th>
                <th>Numéro</th>
                <th>Email</th>
                <th>Académie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('back_admin_home') }}">retour</a>
{% endblock %}

{% block javascripts %}

    <script type="text/javascript">
        var listUrl = '{{ path('api_schools_list')|escape('js') }}';        
    </script>
    <script type="text/javascript" src="{{ asset("js/back/schoolDatatable.js") }}"></script>
{% endblock %}