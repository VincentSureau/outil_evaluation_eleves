{% extends 'base.html.twig' %}

{% block title %}School{% endblock %}

{% block body %}
    <h1>School</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ school.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ school.name }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ school.number }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ school.email }}</td>
            </tr>
            <tr>
                <th>Academy</th>
                <td>{{ school.academy }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_school_index') }}">back to list</a>

    <a href="{{ path('admin_school_edit', {'id': school.id}) }}">edit</a>

    {{ include('back/school/_delete_form.html.twig') }}
{% endblock %}
