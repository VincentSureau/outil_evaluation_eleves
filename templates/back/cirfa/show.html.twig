{% extends 'base.html.twig' %}

{% block title %}Cirfa{% endblock %}

{% block body %}
    <h1>Cirfa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cirfa.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ cirfa.city }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ cirfa.number }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cirfa.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_cirfa_index') }}">retour</a>

    <a href="{{ path('admin_cirfa_edit', {'id': cirfa.id}) }}">edit</a>

    {{ include('back/cirfa/_delete_form.html.twig') }}
{% endblock %}
