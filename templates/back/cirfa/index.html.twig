{% extends 'base.html.twig' %}

{% block title %}Cirfa index{% endblock %}

{% block body %}
    <h1>Liste des CIRFA</h1>
    
    <div class="text-right">
        <button class="btn btn-primary m-bot-10">
            <i class="fas fa-plus"></i>
            <a class="text-white" href="{{ path('admin_cirfa_new') }}">Ajouter un CIRFA</a>
        </button>
    </div>

    <table id="students_list" class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Ville</th>
                <th>Téléphone</th>
                <th>E-mail</th>
                <th>actions</th>
            </tr>
        </thead>
        <tfoot>

        </tfoot>
        <tbody>
        </tbody>
    </table>

    <a href="{{ path('back_admin_home') }}">retour</a>

{% endblock %}

{% block javascripts %}

    <script type="text/javascript">
        var listUrl = '{{ path('api_cirfas_list')|escape('js') }}';     
    </script>
    <script type="text/javascript" src="{{ asset("js/back/cirfaDatatable.js") }}"></script>
{% endblock %}