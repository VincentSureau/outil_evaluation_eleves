{% extends 'base.html.twig' %}

{% block title %}Competence{% endblock %}

{% block body %}
    <h1>Competence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ competence.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ competence.reference }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ competence.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_sn_competence_index') }}">back to list</a>

    <a href="{{ path('admin_sn_competence_edit', {'id': competence.id}) }}">edit</a>

    {{ include('back/sncompetence/_delete_form.html.twig') }}
{% endblock %}
