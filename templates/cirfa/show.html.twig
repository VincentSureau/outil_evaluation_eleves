{% extends 'base.html.twig' %}

{% block title %}Cirfa{% endblock %}

{% block body %}
    <h1>Cirfa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cirfa.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ cirfa.city }}</td>
            </tr>
            <tr>
                <th>Number</th>
                <td>{{ cirfa.number }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ cirfa.email }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cirfa_index') }}">back to list</a>

    <a href="{{ path('cirfa_edit', {'id': cirfa.id}) }}">edit</a>

    {{ include('cirfa/_delete_form.html.twig') }}
{% endblock %}
