{% extends 'base.html.twig' %}

{% block title %}Edit Cirfa{% endblock %}

{% block body %}
    <h1>Edit Cirfa</h1>

    {{ include('cirfa/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('cirfa_index') }}">back to list</a>

    {{ include('cirfa/_delete_form.html.twig') }}
{% endblock %}
